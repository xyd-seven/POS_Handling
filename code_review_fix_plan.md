# BUG 与性能优化修改方案

## 1. 目标

基于 `code_review_bugs_and_performance.md` 中的走读结论，分三阶段修复当前代码中的正确性风险，并对高频串口、回放、大日志绘图场景做必要性能优化。

本方案只定义修改路径，不直接修改业务代码。

## 2. 已确认产品决策

- 回放滑块 seek 需要恢复“从起点到目标时刻的累计轨迹”。
- 高倍速回放必须保证数据完整解析，不能为了速度跳过中间分包。
- 跨午夜分段按同一次连续测试处理。
- 控制台不需要保留大量历史行，优先保证可靠稳定。

## 3. 总体原则

- 优先修复会影响数据正确性的 BUG，再处理性能优化。
- 每阶段内部仍按小步修改，完成一组验证一组。
- 保持现有 UI、数据结构和对外行为兼容。
- 不做大规模重构，不升级依赖，不改变核心架构。
- 涉及回放核心结构、绘图策略的优化单独验证，避免和正确性修复混在一起。

## 4. 三阶段实施方案

### 阶段一：基础可靠性修复

目标：先处理低风险、范围小、收益明确的问题，为后续时间链路和回放修复降低干扰。

#### FIX-01：修复 `CNoPlotCanvas` 初始化缩进问题

- 对应问题：`BUG-01`
- 涉及文件：`main.py`
- 修改范围：`CNoPlotCanvas.__init__()`、`CNoPlotCanvas._handle_delayed_resize()`
- 修改思路：
  - 将坐标轴基础样式、`flag_paths`、`flag_images`、`rendered_sats`、`motion_notify_event` 绑定放回 `__init__()`。
  - `_handle_delayed_resize()` 只保留尺寸计算、`set_size_inches()`、`draw_idle()`。
  - 增加事件连接 ID，例如 `self._motion_cid`，避免重复绑定。
- 验证方式：
  - 启动后不手动拖动窗口，直接加载/回放包含 GSV 的数据，确认 C/N0 图国旗与 hover 正常。
  - 连续拖动窗口，确认无重复 tooltip 触发、无明显卡顿。
- 风险：低。属于初始化位置修正，不改变业务数据。

#### FIX-02：录制写入失败时给出明确提示并停止录制

- 对应问题：`BUG-07`
- 涉及文件：`main.py`
- 修改范围：`handle_serial_read()`，必要时补充 `stop_recording_with_error()` 小函数。
- 修改思路：
  - 捕获 `record_file.write(data)` 异常后，关闭文件句柄。
  - 将 `record_file`、`record_filepath` 清空，并取消 `cb_record` 勾选。
  - 通过状态栏和弹窗提示录制失败，错误内容保留原始异常文本。
  - 避免在高频 read 回调中重复弹窗，可增加一次性错误保护标记。
- 验证方式：
  - 使用不可写路径或模拟关闭文件句柄后触发写入，确认 UI 不再显示录制中。
  - 正常录制路径仍可保存文件。
- 风险：低。只增强异常处理。

#### PERF-FIX-01：控制台文本容量限制改为文档行数限制

- 对应问题：`PERF-01`
- 涉及文件：`main.py`
- 修改范围：`safe_append_console()`、`parse_raw_chunk()`、初始化逻辑。
- 修改思路：
  - 使用 `self.txt_console.document().setMaximumBlockCount(N)` 限制行数。
  - 移除每帧 `toPlainText()` 长度检查。
  - 因已确认控制台不需要保留很多行，默认行数可设置为较小且稳定的值，例如 500 或 1000 行。
- 验证方式：
  - 高频回放时控制台不会无限增长。
  - 滚动跟随逻辑仍正常。
- 风险：低。会改变控制台历史保留长度，但符合已确认决策。

### 阶段二：时间与回放正确性修复

目标：统一处理所有会影响统计、绘图、导出、回放结果正确性的逻辑。该阶段是核心阶段，建议配套测试样例后实施。

#### FIX-03：统一分段时间范围解析，支持跨午夜连续测试

- 对应问题：`BUG-02`、`BUG-06`
- 涉及文件：`main.py`，可能涉及 `ui_main.py`
- 修改范围：
  - `recompute_all()`
  - `on_export_raw_clicked()`
  - `on_export_gga_clicked()`
  - 可新增私有辅助函数，例如 `_is_time_in_segment()`、`_get_segment_epoch_ranges()`
- 修改思路：
  - 保持 UI 当前允许跨午夜的交互。
  - 将分段时间过滤逻辑集中到 `MainWindow` 内的辅助函数。
  - 对普通范围使用 `start_sec <= t <= end_sec`。
  - 对跨午夜范围按同一次连续测试处理，支持午夜前后数据连续进入同一分段。
  - 对已经 `unwrap_times()` 的文件历元，需要将分段时间映射到同一连续时间轴，避免跨天日志被 `% 86400` 截断。
  - 导出原始数据和 GGA 转换复用同一判断规则，避免统计和导出不一致。
- 验证方式：
  - 构造 `23:50:00 -> 00:10:00` 的分段，确认统计、绘图有数据。
  - 同一分段导出原始日志和 GGA，确认包含午夜前后两段数据。
  - 普通日内分段行为保持不变。
- 风险：中。影响统计、绘图、导出三条链路。

#### FIX-04：优化 GGA/GSA/POGOS 状态关联键

- 对应问题：`BUG-05`
- 涉及文件：`main.py`
- 修改范围：`LogParserThread.run()`
- 修改思路：
  - 不再用 `int(utc_time_sec) % 86400` 作为唯一键。
  - 导入阶段保留原始顺序和完整 `utc_time_sec`。
  - 对 GGA/GSA 状态建立按时间排序的列表。
  - 对 POGOS/PODRS 关联最近的 GGA/GSA 状态，设置最大容忍窗口，例如 1 秒。
  - 若找不到近邻状态，则保持原始字段或默认值，不强行覆盖。
- 验证方式：
  - 同一秒内多条不同质量状态数据，确认不会全部被最后一条覆盖。
  - 跨午夜日志中，同一秒-of-day 不会互相串扰。
- 风险：中。会影响历史日志导入后的质量、DOP、卫星数字段。

#### FIX-05：高倍速回放不跳过数据解析

- 对应问题：`BUG-03`
- 涉及文件：`main.py`
- 修改范围：`replay_tick()`
- 修改思路：
  - 保留高倍速控制台节流：`_disable_console_append = True`。
  - 移除直接跳到 `target_index - 20` 后只解析最后 20 包的行为。
  - 对所有应处理分包执行 `parse_raw_chunk()`，但限制 `recompute_all()` 和 `update_cno_chart()` 刷新频率。
  - 若一次 tick 待处理块过多，可分批处理，并在下一 tick 继续追赶，避免单次 UI 阻塞过久。
- 验证方式：
  - 使用相同日志分别以 1x 和 10x 回放，结束后定位历元数量一致。
  - 高倍速下控制台文本不爆量，图表仍按节流刷新。
- 风险：中。高倍速时 CPU 负载可能上升，需要与刷新节流一起验证。

#### FIX-06：seek 恢复从起点到目标时刻的累计轨迹

- 对应问题：`BUG-04`
- 涉及文件：`main.py`
- 修改范围：`on_slider_released()`、可能涉及 `load_replay_file()`
- 修改思路：
  - 释放滑块后清空当前实时回放状态。
  - 从第 0 个分包解析到目标 `replay_index`，期间禁用控制台输出。
  - 解析完成后只做一次 `recompute_all()` 和 `update_cno_chart()`。
  - 后续如 seek 性能不足，再升级为分包快照方案：每隔固定块数保存状态，seek 时从最近快照恢复。
- 验证方式：
  - seek 到中段后，轨迹点数量等于起点到目标时刻累计数据。
  - seek 后继续播放，后续轨迹连续增长。
- 风险：中到高。大文件 seek 可能耗时，需要结合后续 offset 索引或快照优化。

### 阶段三：绘图与大数据性能优化

目标：在正确性修复稳定后，减少高频实时、大日志回放、图表导出时的卡顿和内存压力。该阶段可按收益拆成多个小任务执行。

#### PERF-FIX-02：实时统计与图表刷新节流

- 对应问题：`PERF-02`
- 涉及文件：`main.py`
- 修改范围：`handle_serial_read()`、初始化新增定时器或时间戳。
- 修改思路：
  - 数据解析仍实时执行。
  - `recompute_all()` 改为按固定周期执行，例如 500ms 或 1000ms。
  - 可以复用 `last_recompute_time`，或新增 `live_recompute_timer`。
- 验证方式：
  - 串口实时数据持续进入 `realtime_raw_epochs`。
  - 指标和图表按节流周期刷新，无明显 UI 卡顿。
- 风险：低到中。仪表盘实时感会略微降低，但数据完整性不受影响。

#### PERF-FIX-03：实时窗口使用 `deque(maxlen=...)`

- 对应问题：`PERF-03`
- 涉及文件：`main.py`
- 修改范围：初始化、`process_live_epoch()`、`recompute_all()`。
- 修改思路：
  - `realtime_raw_epochs` 改为 `deque(maxlen=6000)`。
  - 需要绘图或计算时再转换为 list。
  - `parsed_epochs` 中 `COM_REALTIME` 的维护尽量避免每帧全量过滤。
- 验证方式：
  - 实时窗口长度保持上限。
  - GGA、POGOS、PODRS 数据源切换后分段数据正确。
- 风险：中。涉及数据容器类型变化，需检查所有调用方。

#### PERF-FIX-04：回放分包改为 offset 索引，降低内存占用

- 对应问题：`PERF-04`
- 涉及文件：`main.py`
- 修改范围：`load_replay_file()`、`replay_tick()`、`on_slider_released()`。
- 修改思路：
  - `replay_blocks` 从 `(time, block_bytes)` 改为 `(time, offset, length)`。
  - 播放或 seek 时打开文件并 `seek(offset)` 读取对应长度。
  - 对纯二进制文件按固定 chunk 建索引，对 NMEA 文件按时间块建索引。
- 验证方式：
  - 大日志载入内存占用明显降低。
  - 回放、暂停、停止、seek 行为保持一致。
- 风险：中到高。涉及回放核心数据结构，建议单独实施。

#### PERF-FIX-05：C/N0 图跳过无变化重绘

- 对应问题：`PERF-05`
- 涉及文件：`main.py`
- 修改范围：`update_cno_chart()`、`CNoPlotCanvas.render_cno()`。
- 修改思路：
  - 先计算 `gsv_satellites`、`used_satellites`、`has_gsa` 的轻量快照。
  - 与上次快照一致时跳过 `render_cno()`。
  - 保留 resize 后强制重绘。
- 验证方式：
  - 无新 GSV/GSA 时 C/N0 不重复重绘。
  - 新卫星、新频点、SNR 变化时正常刷新。
- 风险：中。快照字段遗漏会导致图表不刷新。

#### PERF-FIX-06：报告导出增加进度提示

- 对应问题：`PERF-06`
- 涉及文件：`main.py`
- 修改范围：`on_export_report_clicked()`
- 修改思路：
  - 短期增加 `QProgressDialog`，每渲染一张图更新进度。
  - 暂不优先放入后台线程，避免 Matplotlib 与 Qt canvas 线程安全风险。
- 验证方式：
  - 大数据导出时用户能看到进度。
  - 导出的 Word 图片仍完整。
- 风险：低到中。

#### PERF-FIX-07：绘图降采样按画布宽度动态计算

- 对应问题：`PERF-07`
- 涉及文件：`plot_widget.py`
- 修改范围：`draw_epoch_horizontal()`、`draw_epoch_vertical()`、`_draw_sats_subplot()`。
- 修改思路：
  - 用画布像素宽度估算最大有效绘制点数。
  - 保留 max/min 形态的降采样策略，避免丢失尖峰。
  - marker 显示阈值与降采样后点数关联。
- 验证方式：
  - 小数据图形细节不变。
  - 大数据图形渲染更快，峰值不丢失。
- 风险：中。需要用大数据样例做视觉对比。

## 5. 测试计划

### 单元 / 逻辑测试

- `CNoPlotCanvas` 初始化后属性存在：`flag_images`、`rendered_sats`、事件连接 ID。
- 跨午夜时间判断：普通范围、跨午夜范围、边界等于开始/结束时间。
- GGA/GSA/POGOS 状态关联：同秒多状态、跨午夜、多天同秒。
- 高倍速回放：1x 与 10x 回放后的定位历元数量一致。
- seek 累计轨迹：跳到中段后，实时缓存点数与从起点解析到目标块一致。

### 手工验证

- 导入普通日志，确认统计和图表不回退。
- 导入跨午夜日志，确认分段统计、图表、导出一致。
- 串口实时接收或回放，确认控制台稳定且不明显卡顿。
- C/N0 图首次显示、窗口 resize、hover tooltip 正常。
- Word/KML/原始/GGA 导出基本流程正常。

## 6. 推荐实施顺序

1. 阶段一：基础可靠性修复。范围小，适合作为第一批改动。
2. 阶段二：时间与回放正确性修复。影响核心结果，建议配套测试后集中处理。
3. 阶段三：绘图与大数据性能优化。可根据实际卡顿和内存压力逐项实施。

## 7. 风险说明

- 阶段二会影响统计、绘图、导出、回放结果，必须配套测试。
- 回放 offset 索引会改变 `replay_blocks` 数据结构，属于中高风险改动，不建议和阶段二正确性修复混在同一次变更中。
- 绘图性能优化需要视觉对比，避免为了速度牺牲关键峰值信息。
