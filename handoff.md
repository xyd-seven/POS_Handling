# Hand Off - VCOM定位精度分析工具交接文档

## 1. 项目目标
基于 PySide6 开发的 GNSS/NMEA 定位精度分析与格式转换桌面工具。支持多协议解析、动静态高精度对比、ENU三向误差、速度对比、误差累积分布 (CDF)、极坐标天空图 (SkyPlot)、3D 空间立体天球穹顶 (3D SkyDome)、载噪比柱状图及 Word 报告导出。当前阶段已完成全界面 100% 彻底无死角浅色模式交付。

---

## 2. 当前状态
- [x] **方向 5：HTML 报告【靶心图刻度与排版 100% 严谨看齐桌面端软件】全面交付上线**：
  1. **刻度整步长对数算法移植**：
     - 彻底摒弃粗暴机械的 0.25/0.5/0.75 四等分算法；
     - 完整移植桌面端 `plot_widget.py` 的**对数指数整步长自适应算法（Nice Step Algorithm）**，同心圆步长严格锁定为 `1/2/5 × 10^n` 整数倍（如 `100m, 200m, 300m, 400m` 或 `10m, 20m`），数值规整周正，与软件端 100% 一致；
  2. **45 度优雅轻量排印（告别厚重白底边框）**：
     - 彻底清除 X 轴正半轴上厚重的白底胶囊和大方框；
     - 将刻度文字以小巧纤细字体（`fontSize: 10, color: tc.axisText`）轻盈浮印在**右下角东南 45 度对角线（`x = r·0.7071, y = -r·0.7071`）**，绝不遮挡真实测试轨迹，避开十字轴与图例；
  3. **四向罗盘简洁化与全屏克隆深度加固**：
     - 方位指示恢复为纯字母 **`N`、`S`、`E`、`W`**，排布在最外圈，彻底消除大边框与顶部图例重叠问题；
     - 放弃函数式 formatter，改用数据驱动的标准字符串模板 **`formatter: '{b}'`**，彻底修复 `JSON.stringify` 抹除函数导致全屏放大出现 `0` 和裸露坐标极值的 Bug！
- [x] **全量测试与回归测试 PASS**，PyInstaller onedir 编译并部署至 `F:\TestTools\pos_handling\dist\main\`。

---

## 3. 当前任务
- 无（靶心图刻度与罗盘已全面优化交付）

---

## 4. 关键设计决策
- 极坐标刻度采用字符串模板 `{b}`，从根本上兼顾了高动态渲染与全屏跨实例深度克隆的物理安全性；
- 遵循软件端 UI 规范，采用东南 45 度斜向标注，最大化利用雷达图四个象限的留白区域。

---

## 5. 修改记录
- exporters/html_exporter.py
- main.py
- walkthrough.md
- handoff.md

---

## 6. 已知问题
无已知未修复的 P0、P1、P2 级别问题。

---

## 7. 下一步任务
1. [方向 4 - 优先级高] u-blox UBX 原生二进制协议 (`.ubx`) 直接拖入解析支持。
2. [方向 4 - 优先级高] RTCM 3.x 差分报文链路监视面板（基站坐标与电文更新率）。
3. [第二梯队 - 优先级高] Phase 3: 百万级 LOD 视口动态降采样渲染。
4. [第二梯队 - 优先级中] TTFF 首次定位时间与 RTK 重捕获 (Re-Fix Time) 自动化分析。

---

## 8. 测试状态
- 对数整步长算法验证（niceStep = 1.0, 2.0, 5.0）：PASS
- 东南 45 度轻量排印与字符串模板 {b} 验证：PASS
- 全屏放大克隆安全与无异常 0 数值验证：PASS
- 纯字母 N/S/E/W 罗盘标线验证：PASS
- PyInstaller onedir 编译与部署：PASS

---

## 9. 编译打包与交付规范 (Packaging & Delivery Convention)
- **编译命令**：`py -m PyInstaller main.spec -y`
- **交付目标路径**：直接复制生成的 `dist/main` 绿色文件夹至 `F:\TestTools\pos_handling\dist\main\`
- **启动路径**：`F:\TestTools\pos_handling\dist\main\main.exe`（无需任何解压，双击即秒开）
