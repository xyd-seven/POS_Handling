# Hand Off - VCOM定位精度分析工具交接文档

## 1. 项目目标
本软件是基于 PySide6 (Qt) 开发的 GNSS/NMEA 定位精度分析与格式转换的桌面工具。当前阶段已完成串口控制台的优化与版本弹窗、主动断电超时和未定位状态重置清空，最终目标是提供一个高性能、轻量、高可读的定位质量分析与转换工具。

---

## 2. 当前状态
- `[x]` 彻底解决自动滚动不生效问题，并建立滚动条位置与自动滚动复选框的双向联动
- `[x]` 解决手动切换数据源后下一秒自动切回 GGA 的 Bug，并统一 GOS/DRS 继承 GGA 状态以刷新仪表盘
- `[x]` 修复时间轴对齐模式下串口实时分段误弹出 X 轴对齐限制的警告
- `[x]` 优化串口刷新按钮，使用矢量绘制防止图标显示为方框，并支持长串口名称下拉与悬停 ToolTip 全名显示
- `[x]` 解决点击“查询版本”按钮后，多行版本回显（$BKCHIP、$FWVER 等）无法弹出对话框显示的问题
- `[x]` 解决弹出框内版本信息无法用鼠标选中及通过键盘复制的问题
- `[x]` 解决只在主动点击查询版本时才弹窗，平常状态下过滤常态打印，不弹窗打扰用户的问题
- `[x]` 解决点击复位指令、断开连接时，实时定位基本状态卡片没有重置的遗留残留数据问题
- `[x]` 解决设备主动断电（串口数据断流）或未定位（质量为 0）时，界面仍残留历史旧坐标造成的误导问题
- `[x]` 解决下发冷启、热启等复位指令时导致硬件重启卡顿的 `NameError: crc16_ccitt` 报错问题（方案 A，简化为只发明文指令）
- `[/]` 载噪比（C/No）柱状图显示功能的开发（已新建分支，准备进行）

---

## 3. 当前任务
- **问题描述**：为实时串口调试界面增加载噪比（C/No）柱状图显示，直观展示当前可见卫星的信号强度。
- **涉及模块**：实时数据流解析、图表渲染与 UI 布局。
- **涉及文件**：[main.py](file:///C:/Users/Administrator/.gemini/antigravity/worktrees/pos_handling/configure-project-hand-off-rules/main.py), [ui_main.py](file:///C:/Users/Administrator/.gemini/antigravity/worktrees/pos_handling/configure-project-hand-off-rules/ui_main.py), [gnss_parser.py](file:///C:/Users/Administrator/.gemini/antigravity/worktrees/pos_handling/configure-project-hand-off-rules/gnss_parser.py)

---

## 4. 关键设计决策
- **延迟合并弹窗机制**：针对串口数据流式分批到达特征，设计了“多行缓存 + 500ms 静默超时”的延迟合并弹窗方案。收到版本报文后不立即弹窗，而是累加并重置 500ms 单次定时器，待流静默 500ms 后一次性合并弹出汇总消息。
- **主动版本查询控制**：引入 `waiting_for_version` 标志位及 3 秒安全超时器。仅在用户手动触发“查询版本”按钮后的 3 秒内对 NMEA 及 BK 二进制流进行版本帧捕获，其余时段直接过滤跳过，彻底避免常态数据流中的版本报文干扰正常操作。
- **数据断流与质量零的重置机制**：设计了 2.5 秒的实时超时定时器 `realtime_timeout_timer` 监听串口定位流。若发生设备物理断电或断流，2.5 秒后自动清空 UI 残留；同时，在实时数据流 `quality == 0` 时拦截更新，将坐标重置为未定位，仅保留 UTC 时间戳，避免残留历史坐标误导用户。
- **复位指令简化（方案 A）**：移除冷启/热启指令中的二进制 `bk_cmd` 发送，仅保留纯明文 NMEA 复位指令。规避了高频下发双指令引起芯片串口状态机混乱死锁卡顿，实现即时重启和重新打印。

---

## 5. 修改记录
- `main.py`
- `ui_main.py`
- `gnss_parser.py`
- `settings_dialog.py`

---

## 6. 已知问题
无已知未修复的 P0、P1、P2 级别问题。

---

## 7. 下一步任务
1. **[优先级 - 高] 载噪比（C/No）数据提取**：在 `gnss_parser.py` 中扩展 GSV 语句解析，提取各卫星 PRN 和载噪比强度值。
2. **[优先级 - 高] 载噪比柱状图绘制**：在 `ui_main.py` 增加柱状图绘图区，利用 Matplotlib 在 `main.py` 中实现动态柱状图渲染。

---

## 8. 测试状态
- **串口多行版本信息静默合并弹窗测试**：PASS
- **弹窗内版本文本选择与 Ctrl+C 复制测试**：PASS
- **只在主动点击查询时触发弹窗，正常接收数据流不弹窗测试**：PASS
- **下发冷启/热启复位指令即时响应、日志无卡顿重启测试**：PASS
- **断开串口连接、下发复位指令时界面定位数据重置清空测试**：PASS
- **物理断电拔线（数据流中断 2.5 秒后）界面定位卡片重置清空测试**：PASS
- **搜星未定位状态（质量为 0）时，界面仅更新 UTC 时间，经纬度等字段变灰清空测试**：PASS

---

## 9. 对下一位 Agent 的要求
- 先阅读相关实现
- 不扫描整个项目
- 非必要不读取大文件
- 保持现有架构
- 保持现有代码风格
- 修改前分析影响范围
- 遵守《AI Agent 工作准则》

发现以下情况立即停止并询问用户：
- 需求不明确
- 涉及数据库结构调整
- 涉及接口协议变更
- 涉及跨模块重构
- 涉及架构调整
- 无法确认影响范围
