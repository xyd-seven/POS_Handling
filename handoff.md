# Hand Off - VCOM定位精度分析工具交接文档

## 1. 项目目标
基于 PySide6 开发的 GNSS/NMEA 定位精度分析与格式转换桌面工具。支持多协议解析、动静态高精度对比、ENU三向误差、速度对比、误差累积分布 (CDF)、极坐标天空图 (SkyPlot)、3D 空间立体天球穹顶 (3D SkyDome)、载噪比柱状图及 Word 报告导出。当前阶段已完成全界面 100% 彻底无死角浅色模式交付。

---

## 2. 当前状态
- [x] **方向 5：HTML 报告【1Hz / 10Hz 混排平滑插值对齐与顶部多文件细化 KPI 卡片】全面交付上线**：
  1. **1Hz 与 10Hz 混排断线彻底根治**：
     - 时间戳归一化（毫秒级统一对齐格式 `hh:mm:ss.sss`）；
     - `np.interp` 高精度时间插值算法，将各分段数据平滑投影至全局时间轴，有效测试区间内点点连续；
     - 前端 ECharts 核心折线全部显式配置 `connectNulls: true`，平滑穿透采样间隙；
  2. **顶部 KPI 卡片多文件细化展示**：
     - 单分段保留大字突出展示；
     - 多分段时大字显示综合均值（最大偏差显示最大值），并在卡片下方渲染专属对比胶囊（分段颜色圆点、分段名称、分段独立指标数值），彻底解决无法区分具体文件误差的问题；
- [x] **方向 4：GNSS 解析层【RMC 语法结构防伪机制（零人为上限、纯协议语法清洗错位乱码）】全面交付上线**：
  1. **半球指示符严格语法校验**：
     - `nmea_to_deg` 底层与 RMC 解析器硬性要求纬度半球必须为 `'N'/'S'`，经度半球必须为 `'E'/'W'`；
     - 精准截杀因串口丢逗号导致的半球丢失与数字粘连（如 `COM92` 中的 `'34'`、`'2434'` 导致 GPS 周内秒错位顶替速度产生 193451 m/s 的大假尖峰）；
  2. **字段左移错位结构识别**：
     - 识别速度丢失导致航向角（COG）与日期（Date）字段整体左移的异常特征，严防航向角（如 317°）冒充速度（317 节）；
  3. **零人为上限兜底（遵循纯正协议语法过滤）**：
     - 完全未硬编码任何速度上限，纯粹依靠协议语法标准截杀坏帧，保证高速运动载体测试数据 100% 真实客观；
  4. **实测验证**：针对 `0903/单边楼` 数据，最大速度误差由 `193451 m/s` 彻底归零，回落到 `0.005m/s ~ 0.057m/s` 的真实平稳水平！
- [x] **全量测试与回归测试 PASS**，PyInstaller onedir 编译并部署至 `F:\TestTools\pos_handling\dist\main\`。

---

## 3. 当前任务
- 无（HTML 报告 1Hz/10Hz 插值连续对齐与顶部多文件对比 KPI 卡片已全面交付）

---

## 4. 关键设计决策
- 坚决不设速度硬阈值，从 NMEA 半球指示符与字段相对结构切入，杜绝暴力限速可能引发的边界争议。

---

## 5. 修改记录
- gnss_parser.py
- main.py
- walkthrough.md
- handoff.md

---

## 6. 已知问题
无已知未修复的 P0、P1、P2 级别问题。

---

## 7. 下一步任务
1. [方向 4 - 优先级高] u-blox UBX 原生二进制协议 (`.ubx`) 直接拖入解析支持。
2. [方向 4 - 优先级高] RTCM 3.x 差分报文链路监视面板（基站坐标与电文更新率）。
3. [第二梯队 - 优先级高] Phase 3: 百万级 LOD 视口动态降采样渲染。
4. [第二梯队 - 优先级中] TTFF 首次定位时间与 RTK 重捕获 (Re-Fix Time) 自动化分析。

---

## 8. 测试状态
- nmea_to_deg 半球严格指示符（N/S 与 E/W）验证：PASS
- RMC 语法结构防伪与字段错位（34, 2434, 错位日期）拦截验证：PASS
- 单边楼 4 组实测数据验证（193451 m/s 彻底归零）：PASS
- PyInstaller onedir 编译与部署：PASS

---

## 9. 编译打包与交付规范 (Packaging & Delivery Convention)
- **编译命令**：`py -m PyInstaller main.spec -y`
- **交付目标路径**：直接复制生成的 `dist/main` 绿色文件夹至 `F:\TestTools\pos_handling\dist\main\`
- **启动路径**：`F:\TestTools\pos_handling\dist\main\main.exe`（无需任何解压，双击即秒开）
