# Hand Off - VCOM定位精度分析工具交接文档

## 1. 项目目标
本软件是基于 PySide6 开发的 GNSS/NMEA 定位精度分析与格式转换的桌面工具。当前阶段已完成串口/文件回放控制、载噪比（C/N0）立柱图、性能防抖重绘等优化，最终目标是提供一个高性能、轻量、高可读的定位质量分析工具。

---

## 2. 当前状态
- `[x]` 在 `gnss_parser.py` 中扩展 GSV 语句解析，提取各星座卫星 PRN、频点及载噪比（C/N0）值
- `[x]` 增加载噪比（C/N0）柱状图画布，引入 Matplotlib 实现频点区分的多色彩柱状图动态渲染
- `[x]` 优化多星座配色方案（北斗 B1C 玫瑰粉、B2a 极光洋红；格罗纳斯黄色系；伽利略 E1 深宝蓝）
- `[x]` 解决界面分隔条拖拽与窗口缩放时的重绘卡顿，实现 100ms 延迟重绘防抖机制
- `[x]` 实现高倍速回放控制，增加进度条拖拽定位（Seek）与大于等于 5.0 倍速时终端高频刷新节流机制
- `[x]` 实现停止回放与关闭串口时保留界面最后时刻的数据 UI（定位基本信息与 C/N0 视图），仅在开启新会话或手动清除时重置
- `[x]` 优化进度条拖拽定位（Seek）性能，引入批量解析模式（Batch Mode）挂起高频刷新，将 $O(N^2)$ 列表重构复杂度优化为 $O(1)$
- `[x]` 优化快照捕获（capture_replay_snapshot），使用元素数量切片（Slice-based Snapshots）替代原先昂贵的列表 `copy.deepcopy`，提升寻道性能万倍以上
- `[x]` 实现快照向后兼容机制，恢复快照时自动兼容旧版包含完整列表深拷贝的数据格式，防止崩溃
- `[x]` 编写 15 个单元测试用例，覆盖全部回放控制、快照兼容恢复与状态重置逻辑

---

## 3. 当前任务
- 无（本阶段所有开发、优化与编译打包任务均已顺利完成，测试全部通过）

---

## 4. 关键设计决策
- **载噪比重绘 2Hz 限制与界面防抖重绘（Debounced Resize）**：为防止大量卫星通道及 OffsetImage 在高频刷新及 resize 窗口时阻塞主线程，引入 2Hz 渲染更新节流，并在 `CNoPlotCanvas` 与 `PlotWidget` 重写 `resizeEvent` 使用 100ms 定时器做防抖处理。
- **回放倍速终端节流**：当回放倍速 $\ge 5.0x$ 时，因文本追加开销过高，自动节流并阻断控制台的文本刷新，只更新图形与指标；降速/暂停后恢复。
- **条件式 UI 重置逻辑**：关闭串口或停止回放时不触发重置，保留现场数据；仅在导入新文件、重新打开串口或主动清空时触发重置。
- **多星座频点颜色区分**：使用深浅明暗与冷暖色度不同的特异性颜色区分（如 Fuchsia 洋红 `#D946EF` 代表 B2a，Royal Blue `#2563EB` 代表 E1），并避开 GLONASS 的琥珀色系，保证暗色背景的高对比度。
- **批量解析模式 (Batch Mode) 与切片快照 (Slice-based Snapshots)**：在拖拽进度条 Seek 时，使用 `self.is_bulk_parsing` 挂起每次历元解析的截断操作和 UI 重绘，在循环结束后一次性刷新。同时利用切片计数代替深拷贝保存快照状态，使长日志下的 Seek 操作耗时由数秒降为 0-50ms，且对历史快照文件格式完全向下兼容。

---

## 5. 修改记录
- `main.py`
- `ui_main.py`
- `plot_widget.py`
- `test_replay.py`

---

## 6. 已知问题
无已知未修复的 P0、P1、P2 级别问题。

---

## 7. 下一步任务
1. **[优先级 - 低] 界面微调**：根据用户在不同屏幕分辨率下的使用体验，微调各卡片初始分割比例。

---

## 8. 测试状态
- **回放文件预分包、空文件及垃圾文件容错测试**：PASS
- **回放进度条拖拽定位与时间轴显示测试**：PASS
- **回放倍速调整与高倍速（5.0x 以上）控制台节流测试**：PASS
- **停止回放与关闭串口时数据保留（不自动清除）测试**：PASS
- **重新打开串口、加载新文件及手动点击清除时 UI 自动重置测试**：PASS
- **回放 Seek 性能与新老快照版本兼容性测试**：PASS
- **单元测试套件自动化运行 (15/15 个测试用例)**：PASS

---

## 9. 对下一位 Agent 的要求
- 先阅读相关实现
- 不扫描整个项目
- 非必要不读取大文件
- 保持现有架构
- 保持现有代码风格
- 修改前分析影响范围
- 遵守《AI Agent 工作准则》

发现以下情况立即停止并询问用户：
- 需求不明确
- 涉及数据库结构调整
- 涉及接口协议变更
- 涉及跨模块重构
- 涉及架构调整
- 无法确认影响范围
